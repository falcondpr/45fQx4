generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tester {
  id         Int      @id @default(autoincrement())
  name       String?
  username   String
  email      String   @unique
}