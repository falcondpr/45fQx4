generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         Int      @id @default(autoincrement())
  name       String
  email      String
  password   String
  created_at DateTime @default(now())
}

model Message {
  id                  Int      @id @default(autoincrement())
  id_user_transmitter Int
  id_user_receiver    Int
  content             String
  created_at          DateTime @default(now())
}

model Requests {
  id         Int      @id @default(autoincrement())
  id_product String
  status     Boolean
  created_at DateTime @default(now())
}

model MessageComment {
  id         Int      @id @default(autoincrement())
  content    String
  id_vendor  String
  id_buyer   String
  created_at DateTime @default(now())
}
