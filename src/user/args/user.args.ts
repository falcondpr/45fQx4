export const AVATAR_SCHEMA = {
  url: String,
  isApproved: Boolean,
  updatedAt: Date,
};

export const BANNER_SCHEMA = {
  url: String,
  updatedAt: Date,
};

export const IS_VERIFIED_SCHEMA = {
  isValid: String,
  updatedAt: Date,
};

export const IS_PREMIUM_SCHEMA = {
  isValid: Boolean,
  updatedAt: Date,
};
